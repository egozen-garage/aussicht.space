import { CheckArtistHoverPipe } from './check-artist-hover.pipe';

describe('CheckArtistHoverPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckArtistHoverPipe();
    expect(pipe).toBeTruthy();
  });
});
