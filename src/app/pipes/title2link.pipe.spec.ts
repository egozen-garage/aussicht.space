import { Title2linkPipe } from './title2link.pipe';

describe('Title2linkPipe', () => {
  it('create an instance', () => {
    const pipe = new Title2linkPipe();
    expect(pipe).toBeTruthy();
  });
});
