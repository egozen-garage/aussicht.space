import { AddimagesDirective } from './addimages.directive';

describe('AddimagesDirective', () => {
  it('should create an instance', () => {
    const directive = new AddimagesDirective();
    expect(directive).toBeTruthy();
  });
});
