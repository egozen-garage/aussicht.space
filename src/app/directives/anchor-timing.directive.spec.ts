import { AnchorTimingDirective } from './anchor-timing.directive';

describe('AnchorTimingDirective', () => {
  it('should create an instance', () => {
    const directive = new AnchorTimingDirective();
    expect(directive).toBeTruthy();
  });
});
